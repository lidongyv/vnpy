# VNPy环境配置完成！

## ✅ 已安装内容

- **Python版本**: 3.10.19
- **VNPy核心**: 4.3.0
- **策略模块**: CTA策略、CTA回测、数据管理、本地仿真、风险管理
- **关键依赖**: NumPy 2.2.3, Pandas 2.3.3, TA-Lib 0.6.4, PySide6 6.8.2.1

---

## 🚀 启动VNPy

### 方法1：使用启动脚本（推荐）

```bash
cd /Users/hanshuqi/Desktop/vnpy
./run_vnpy.sh
```

### 方法2：直接命令行启动

```bash
eval "$(/opt/homebrew/bin/brew shellenv)"
cd /Users/hanshuqi/Desktop/vnpy
python3.10 examples/veighna_trader/run.py
```

---

## ⚙️ 永久配置环境变量（可选）

为了在任何终端都能直接使用`python3.10`命令，可以将Homebrew路径添加到你的shell配置文件：

**对于zsh（macOS默认）：**

```bash
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
source ~/.zprofile
```

配置后，重新打开终端就可以直接使用：
- `python3.10` - Python 3.10
- `pip3.10` - pip包管理器
- `brew` - Homebrew包管理器

---

## 📦 安装更多组件

### 安装AI量化模块（可选）

```bash
eval "$(/opt/homebrew/bin/brew shellenv)"
python3.10 -m pip install polars scikit-learn lightgbm torch alphalens-reloaded -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 安装数据服务（选择一个）

**米筐RQData：**
```bash
python3.10 -m pip install vnpy_rqdata -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**迅投XT数据：**
```bash
python3.10 -m pip install vnpy_xt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**TuShare数据：**
```bash
python3.10 -m pip install vnpy_tushare -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 安装CTP交易接口（期货交易）

根据文档，Mac系统的CTP接口需要从源码编译：

```bash
cd ~
mkdir github
cd github
git clone https://github.com/vnpy/vnpy_ctp.git
cd vnpy_ctp
python3.10 -m pip install -e .
```

**注意**：编译完成后需要手动信任两个动态库文件（参考文档）。

---

## 📚 下一步学习

1. **运行测试示例**
   ```bash
   cd /Users/hanshuqi/Desktop/vnpy/examples
   ```

2. **查看完整教程**
   - 查看：`/Users/hanshuqi/Desktop/vnpy/sonnet/VNPy从零到实战完整指南.md`
   - 按照教程学习数据准备、策略开发、回测优化

3. **官方资源**
   - 官网：https://www.vnpy.com
   - 文档：https://www.vnpy.com/docs
   - 论坛：https://www.vnpy.com/forum

---

## 🔧 常用命令

```bash
# 验证安装
eval "$(/opt/homebrew/bin/brew shellenv)"
python3.10 test_install.py

# 安装新包（使用清华镜像加速）
python3.10 -m pip install 包名 -i https://pypi.tuna.tsinghua.edu.cn/simple

# 更新VNPy
cd /Users/hanshuqi/Desktop/vnpy
git pull
python3.10 -m pip install . -i https://pypi.tuna.tsinghua.edu.cn/simple

# 查看已安装的包
python3.10 -m pip list
```

---

## ❓ 常见问题

### Q: 提示找不到python3.10命令？
**A**: 每次使用前先运行：
```bash
eval "$(/opt/homebrew/bin/brew shellenv)"
```
或者按照上面"永久配置环境变量"的步骤配置。

### Q: pip安装速度慢？
**A**: 使用清华镜像源：
```bash
python3.10 -m pip install 包名 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q: 如何更新Python包？
**A**: 使用upgrade参数：
```bash
python3.10 -m pip install --upgrade 包名 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

**🎉 恭喜！你的VNPy量化交易环境已经配置完成！**
